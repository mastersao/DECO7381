// var keyword = 'Lorem';    // 假设这里的关键词为“关键词1”
// var bodyContent = document.body.innerHTML;  // 获取页面内容
// var contentArray = bodyContent.split(keyword);
// document.body.innerHTML = contentArray.join('<span class="highlight">' + keyword + '</span>');